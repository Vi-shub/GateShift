# Corpus coverage gaps

Generated by comparing `pkg/adapters/nginx` catalog keys against annotations
present under `examples/corpus/`. Refresh after adding fixtures:

```powershell
# See scripts note in examples/corpus/README.md  -  or re-run a local key scan
```

## Catalog completeness (fixtures)

Goal: every **catalog** key appears in at least one corpus fixture.

| Status | Keys |
|--------|------|
| Covered | All other catalog keys |
| Filled by `public/18-catalog-gaps-remaining.yaml` | `cert-manager.io/issuer`, `session-cookie-change-on-failure`, `session-cookie-hash` |

## Uncatalogued but seen in corpus (adapter candidates)

These appear in Traefik / community / public fixtures and show as `[??]` in
`gateshift coverage`. They are **not silent drops** in audit if loaders surface
them as unknown findings via coverage; prioritize by frequency:

### P0  -  common in production

| Annotation | Notes |
|------------|-------|
| `allowlist-source-range` | Alias of whitelist (newer name) |
| `load-balance` | least_conn / ewna / round_robin |
| `affinity-mode` | balanced / persistent |
| `limit-burst-multiplier` | Pairs with limit-rps |
| `canary-by-cookie` / `canary-by-header-value` / `canary-by-header-pattern` / `canary-weight-total` | Canary matrix |
| `auth-response-headers` | Ext-auth companion |
| `enable-opentelemetry` | Observability |
| `proxy-buffer-size` / `proxy-buffers-number` | Proxy tuning |
| `service-upstream` | Upstream mode |
| `mirror-request-body` | Companion to mirror-target |

### P1  -  auth / TLS depth

| Annotation | Notes |
|------------|-------|
| `auth-tls-verify-client` / `auth-tls-verify-depth` / `auth-tls-pass-certificate-to-upstream` / `auth-tls-error-page` | mTLS depth |
| `auth-cache-key` / `auth-cache-duration` / `auth-method` / `auth-realm` / `auth-secret-type` / `auth-snippet` | Auth variants |
| `enable-global-auth` | Global auth toggle |
| `proxy-ssl-name` / `proxy-ssl-server-name` / `proxy-ssl-verify-depth` | Backend TLS detail |

### P2  -  niche

| Annotation | Notes |
|------------|-------|
| `permanent-redirect-code` / `temporal-redirect-code` | Redirect status codes |
| `server-alias` | Extra server_name |
| `upstream-vhost` | Host rewrite to upstream |
| `ssl-ciphers` | TLS cipher suite |
| `websocket-services` / `grpc-backend` | Protocol helpers |
| `custom-headers` / `whitelist-methods` / `limit-whitelist` | Misc |
| `enable-modsecurity` / `enable-owasp-core-rules` | WAF toggles (beside snippet) |
| `proxy-http-version` / `proxy-max-temp-file-size` / `proxy-next-upstream-*` / `proxy-request-buffering` | Proxy edge |
| `cors-max-age` / `session-cookie-domain` | Small companions |
| `affinity-canary-behavior` | Canary + sticky interaction |

## Traefik leftover policy

- **Positive / structural** → `examples/corpus/traefik/`: **Negative / invalid / missing secret** → `examples/corpus/traefik-edge/`

Edge fixtures often score low or fail validate; that is intentional.
